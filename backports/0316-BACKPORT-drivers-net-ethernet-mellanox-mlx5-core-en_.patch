From: Valentine Fatiev <valentinef@nvidia.com>
Subject: [PATCH] BACKPORT:
 drivers/net/ethernet/mellanox/mlx5/core/en_accel/ipsec.h

Change-Id: I905a244ca550d28ff09ea75fd8fceb04cf7a7c14
---
 drivers/net/ethernet/mellanox/mlx5/core/en_accel/ipsec.h | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

--- a/drivers/net/ethernet/mellanox/mlx5/core/en_accel/ipsec.h
+++ b/drivers/net/ethernet/mellanox/mlx5/core/en_accel/ipsec.h
@@ -114,6 +114,7 @@ enum mlx5_ipsec_cap {
 
 struct mlx5e_priv;
 
+#ifdef CONFIG_MLX5_EN_IPSEC
 struct mlx5e_ipsec_hw_stats {
 	u64 ipsec_rx_pkts;
 	u64 ipsec_rx_bytes;
@@ -282,8 +283,6 @@ struct mlx5e_ipsec_pol_entry {
 	struct mlx5_accel_pol_xfrm_attrs attrs;
 };
 
-#ifdef CONFIG_MLX5_EN_IPSEC
-
 void mlx5e_ipsec_init(struct mlx5e_priv *priv);
 void mlx5e_ipsec_cleanup(struct mlx5e_priv *priv);
 void mlx5e_ipsec_build_netdev(struct mlx5e_priv *priv);
