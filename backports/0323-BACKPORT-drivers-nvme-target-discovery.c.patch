From: Karam Ghanayem <kghanayem@nvidia.com>
Subject: [PATCH] BACKPORT: drivers/nvme/target/discovery.c

Signed-off-by: Karam Ghanayem <kghanayem@nvidia.com>
Change-Id: I65ece7bde6e813f5ed8c837b9f4e85130fd66268
---
 drivers/nvme/target/discovery.c | 9 ++++++++-
 1 file changed, 8 insertions(+), 1 deletion(-)

--- a/drivers/nvme/target/discovery.c
+++ b/drivers/nvme/target/discovery.c
@@ -253,6 +253,9 @@ static void nvmet_execute_disc_identify(
 	struct nvmet_ctrl *ctrl = req->sq->ctrl;
 	struct nvme_id_ctrl *id;
 	u16 status = 0;
+#ifdef HAVE_STRSCPY
+	ssize_t res;
+#endif
 
 	if (!nvmet_check_transfer_len(req, NVME_IDENTIFY_DATA_SIZE))
 		return;
@@ -295,7 +298,11 @@ static void nvmet_execute_disc_identify(
 
 	id->oaes = cpu_to_le32(NVMET_DISC_AEN_CFG_OPTIONAL);
 
-	strscpy(id->subnqn, ctrl->subsys->subsysnqn, sizeof(id->subnqn));
+#ifdef HAVE_STRSCPY
+	res = strscpy(id->subnqn, ctrl->subsys->subsysnqn, sizeof(id->subnqn));
+#else
+	strlcpy(id->subnqn, ctrl->subsys->subsysnqn, sizeof(id->subnqn));
+#endif
 
 	status = nvmet_copy_to_sgl(req, 0, id, sizeof(*id));
 
